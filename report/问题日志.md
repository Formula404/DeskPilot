# 问题日志

本日志用于记录项目开发过程中遇到的重大问题、定位过程、解决方案和验证方式。后续遇到影响体验、稳定性、架构或跨模块行为的问题时，必须追加记录，避免同类问题反复出现。

## 2026-07-21 任务执行时读取全局最新页面导致目标漂移

**现象**

用户在标签页 A 提交“总结当前页面”后切换到标签页 B，旧链路会在真正执行工具时重新读取活动页或数据库最新一条浏览器上下文，可能错误处理 B；并发网页任务也存在互相串页风险。

**根因**

前端只提交消息，`context_id` 虽进入 Agent state，但执行节点和浏览器工具没有按它读取；浏览器桥接命令默认目标始终为活动标签页。Overlay 位于前台时，Windows 前台窗口采集还可能误把 DeskPilot 自身当成用户目标。

**解决方案**

- 提交时创建不可变 `ContextSnapshot`，冻结 session、窗口、tab、URL、选区和附件引用，并把快照 ID 持久化到 `task_runs`。
- 浏览器命令强制发送快照绑定的 `tab_id`，返回后同时校验 tab 与 URL；不一致时返回 `BROWSER_TARGET_CHANGED`，禁止退回全局最新页面。
- Agent 增加 `load_task`、`load_snapshot`、`build_context`，所有跨域上下文统一由 `ContextBuilder` 按 intent 和 Token 预算装配。
- Windows 采集发现前台是 DeskPilot Overlay 时，沿 Z-order 选择其后的首个真实用户窗口。
- 增加并发任务隔离、目标漂移、快照绑定、预算、指代解析与 Secret 门禁测试。

**涉及文件**

- `backend/app/context/`
- `backend/app/agent/`
- `backend/app/browser/bridge.py`
- `backend/app/db/connection.py`
- `backend/app/db/repository.py`
- `backend/app/tools/browser/`
- `browser-extension/src/background.ts`
- `frontend/src/api/client.ts`
- `frontend/src/views/Overlay.tsx`
- `backend/tests/test_context_engineering.py`

**验证方式**

- `uv run pytest`（79 passed）
- `frontend: npm run build`
- `browser-extension: npm run build`

**后续注意**

任何网页、窗口或附件工具都不得把“当前/最新”当成生产任务目标。新增目标类型时必须在提交阶段冻结身份、在执行阶段定向访问并校验身份；快照过期只能刷新同一目标，不能静默选择替代目标。

## 2026-07-21 通用对话进度不可见与恢复任务阻塞关闭

**现象**

通用对话直接等待模型响应，等待期间 SSE 没有步骤事件，前端看起来像卡住；服务关闭时会等待启动恢复的长任务自然完成。上下文压缩还会把 `Dr.` 等英文缩写误判为段落边界，工具执行失败后审计记录会丢失已解析参数。

**根因**

- `general_chat` 没有执行与工具节点一致的步骤发布和持久化流程。
- lifespan 对恢复任务只做 `gather`，没有先发出取消。
- 压缩逻辑使用包含英文句点的宽泛正则，并未对超长单段施加硬上限。
- 工具异常处理无条件用原始 JSON 字符串覆盖 `arguments`。

**解决方案**

- 通用对话在模型请求前发布 `task.step.started`，完成或失败后持久化并发布相应步骤。
- 关闭时先取消未完成的恢复任务，再用 `gather(return_exceptions=True)` 回收。
- 只使用空行和无歧义的中英文强终止符切块，保留缩写、小数和 URL，并裁剪超长单段。
- 仅在参数解析尚未成功时记录 `raw_arguments`；执行期异常保留结构化参数。
- 会话消息与快照原本已有 `ON DELETE CASCADE`；另增加显式删除以兼容可能缺失外键的旧数据库。

**涉及文件**

- `backend/app/agent/nodes.py`
- `backend/app/agent/graph.py`
- `backend/app/agent/tool_calling.py`
- `backend/app/core/task_lifecycle.py`
- `backend/app/main.py`
- `backend/app/db/repository.py`
- `frontend/src/api/client.ts`
- `backend/tests/test_context_engineering.py`

**验证方式**

- `uv run pytest backend/tests/test_context_engineering.py backend/tests/test_tools.py -q`（22 passed）
- `uv run pytest -q`（79 passed）
- `frontend: npm run build`

**后续注意**

任何可能等待网络或模型的 Agent 节点都必须先发布可见的运行状态；应用生命周期拥有的后台任务必须支持取消，不能在 shutdown 中无限等待自然结束。

## 记录规则

- 只记录“大问题”：卡死、闪烁、窗口丢失、任务链路中断、架构性重复、性能明显退化、无障碍/安全风险等。
- 每条记录必须包含：日期、现象、根因、解决方案、涉及文件、验证方式、后续注意点。
- 如果同一问题反复出现，应在原条目下追加“复发记录”，不要另开无关联条目。
- 修复代码后应同步更新本日志，不要只在对话中说明。

## 2026-07-08 Overlay 点击悬浮球后首帧闪烁

**现象**

点击悬浮球打开 overlay 时，UI 会先全部出现，再消失，然后才播放输入框上滑和胶囊按钮弹出的入场动画。

**根因**

Tauri overlay 窗口是隐藏但已挂载的 React 视图。此前 `useGSAP` 在隐藏状态下已经播放完入场动画，导致 DOM 上残留可见 inline style。下次窗口 `show()` 时浏览器先绘制上一轮可见状态，然后 GSAP 再把元素设回透明并播放动画，形成首帧闪烁。

**解决方案**

- 为 overlay 入场元素添加 CSS 初始隐藏态。
- Tauri 环境下组件 mount 时只执行隐藏态 reset，不主动播放入场动画。
- 入场动画改由窗口 focus 后触发。
- `resetOverlayMotionTargets()` 关闭后不再把输入框/胶囊按钮 reset 成可见，而是 reset 成下一次入场前的隐藏态。

**涉及文件**

- `frontend/src/views/Overlay.tsx`
- `frontend/src/styles/components.css`

**验证方式**

- `npm run build`
- 手动点击悬浮球，确认 overlay 不再出现“全部出现 -> 消失 -> 入场”的首帧闪烁。

**后续注意**

Tauri 多窗口里“隐藏窗口仍然挂载”是常态。任何入场动画如果依赖初始透明态，都要确保隐藏窗口期间不会提前播完并留下可见 inline style。

## 2026-07-08 Overlay 入场动画被 focus 事件重复触发

**现象**

点击悬浮球后 overlay 会出现一次短暂闪动，像是出现、消失、再真正进入。

**根因**

入场动画同时被 `useGSAP` 初次执行和 `onFocusChanged` 触发。第二次触发会再次执行 `gsap.set(... autoAlpha: 0, y: ...)`，把已经出现的输入框重新放回底部并设为透明。

**解决方案**

- 增加 `openTimelineRef` 保存当前入场 timeline。
- 如果入场 timeline 仍 active，后续 focus 触发直接忽略。
- timeline complete / reset / unmount 时清理 `openTimelineRef`。

**涉及文件**

- `frontend/src/views/Overlay.tsx`

**验证方式**

- `npm run build`
- 连续点击悬浮球打开 overlay，确认入场动画只播放一次。

**后续注意**

窗口 focus、mount、状态变化都可能触发动效。任何打开动画都应有“正在播放”保护，避免重复 `set` 初始态。

## 2026-07-08 悬浮球偶发消失

**现象**

使用过程中悬浮球会自己消失，用户无法再次打开 overlay。

**根因**

打开 overlay 时悬浮球会被 `hide()`，恢复完全依赖 overlay 关闭链路调用 `restoreFloatingBall()`。如果某次关闭、失焦或窗口状态更新没有完整走到恢复逻辑，悬浮球会长期 hidden。

**解决方案**

- 在悬浮球窗口内增加空闲自愈守护：当 overlay 不可见而悬浮球自身 hidden 时，主动 `show()`。
- `restoreFloatingBall()` 增加多次延迟检查，覆盖 Tauri 可见状态更新延迟。

**涉及文件**

- `frontend/src/views/FloatingBall.tsx`
- `frontend/src/views/windowActions.ts`

**验证方式**

- `npm run build`
- 手动打开/关闭 overlay，多次失焦后确认悬浮球能恢复。

**后续注意**

跨窗口 hide/show 不应只依赖单一关闭路径。关键入口窗口需要自愈或状态守护。

### 2026-07-21 复发记录

**现象**

非模态浮窗改造后，打开并关闭一次 overlay，overlay 与悬浮球会一起消失。

**根因**

关闭链路先执行 `overlay.hide()`，再异步调用 `restoreFloatingBall()`。Windows 隐藏 WebView 后会暂停该窗口中的 JavaScript，导致后续恢复悬浮球的 Promise 和延迟检查可能无法继续执行。

首次时序修复后继续复测发现，GSAP 退场还会在隐藏但未卸载的 overlay DOM 上保留 `opacity: 0` 和 `visibility: hidden`。第二次原生窗口虽然成功显示，React 组件不会重新挂载，入场动画也不会自动重播，因此窗口内容完全透明；悬浮球随后正常隐藏，视觉上表现为两个窗口一起消失。

**解决方案**

- 调整跨窗口关闭顺序：先直接 `show()` 悬浮球，再隐藏 overlay。
- 调整跨窗口打开顺序：先显示并聚焦 overlay，再隐藏悬浮球，避免调用方窗口提前暂停。
- overlay 打开失败时，由仍处于活动状态的调用链直接隐藏 overlay 并显示悬浮球，不再依赖隐藏窗口内的延迟任务。
- 关闭时将 overlay 内部形态重置为快速输入，保证第二次唤起的窗口尺寸与内容一致。
- 悬浮球每次显示 overlay 后向目标窗口发送 `overlay-open-request`，overlay 收到事件后强制恢复快速输入形态并重播入场动画，清除上一次退场留下的透明状态。
- Tauri 环境挂载时先将面板设为透明，避免 `show()` 到打开事件之间出现不透明首帧；焦点回调仅在检测到面板仍透明时补进入场动画，作为跨窗口事件丢失时的回退。
- 移除两个同时修改 panel 透明度和 transform 的独立 tween，形态变化只动画内部内容，面板入场由单一 timeline 持有。
- 恢复悬浮球改为关闭 overlay 前同步重试并检查 `isVisible()`；多次失败时保留 overlay，避免两个入口同时消失。
- 移除浮窗四周透明 padding，避免透明 WebView 边缘在 Windows 上呈现黑边。
- overlay 的三种形态统一定位到当前显示器工作区底部中央、任务栏上方。

**涉及文件**

- `frontend/src/views/windowActions.ts`
- `frontend/src/styles/components.css`

**验证方式**

- `npm run build`
- 连续打开、关闭 overlay，确认悬浮球每次都在 overlay 隐藏前恢复。
- 连续打开、关闭至少 5 次，确认每次重新打开都会收到跨窗口事件并恢复可见内容。
- 模拟打开事件未到达，确认 overlay 获得焦点时只在内容仍透明的情况下补播入场动画。
- 检查快速输入、对话和执行控制条三种尺寸均贴合原生窗口边界并位于任务栏上方中央。

**后续注意**

不要在隐藏 Tauri WebView 后依赖该窗口继续完成关键跨窗口操作；恢复入口窗口必须发生在当前窗口隐藏之前。

### 2026-07-22 复发记录

**现象**

Overlay 首次打开显示正常，关闭后再次打开时标题栏和快捷建议全部不可见，只剩输入框，输入与对话功能仍可正常使用。

**根因**

标题栏和快捷建议共用 `panel-item` 入场动画，而输入框不在该动画组内。关闭操作会中断正在进行的入场 timeline；中断时 `panel-item` 可能保留 `opacity: 0` 和 `visibility: hidden`。再次打开使用 `gsap.from()` 时，GSAP 会把元素当前的残留隐藏值作为动画终点，导致它们从隐藏动画到隐藏，无法恢复；输入框未被该动画修改，因此仍然可见。

**解决方案**

- 将 `panel-item` 入场从隐式终点的 `from()` 改为显式 `fromTo()`，保证每次打开都从隐藏状态动画到 `autoAlpha: 1`。
- 入场开始前终止并清理可能残留的退场 tween，避免打开和关闭同时修改面板。
- 退场完成且面板已经透明后，清除所有 `panel-item` 的 transform、opacity 和 visibility 内联样式，为下一次打开建立确定状态。
- 组件清理时同时终止入场 timeline 和退场 tween。

**涉及文件**

- `frontend/src/views/Overlay.tsx`

**验证方式**

- `npm run build`
- 连续快速执行“打开 → 关闭 → 再打开”，确认标题栏、快捷建议和输入框每次都完整出现。

**后续注意**

可中断的 GSAP `from()` 动画不能依赖目标当前的 inline style 作为终点。窗口会被隐藏并复用时，关键可见性动画应使用显式 `fromTo()`，并在隐藏后归一化残留样式。

## 2026-07-08 入场动画不可打断

**现象**

用户误点击悬浮球打开 overlay 后，如果想马上退出，必须等入场动画全部播放完成。入场过程中点击空白或按 Esc 不会立即关闭。

**根因**

`shouldIgnoreClose` 用时间窗口屏蔽所有关闭请求，原本是为了避免 Tauri focus 抖动导致刚打开就被 blur 关闭，但也误拦截了用户主动关闭。

**解决方案**

- `useWindowLifecycle` 增加关闭原因：`manual`、`escape`、`blur`。
- 入场保护只忽略 `blur`，不忽略用户点击空白或 Esc。
- 退场动画开始时 kill 当前入场 timeline 和相关 tweens，从当前动画状态直接接退场。

**涉及文件**

- `frontend/src/hooks/useWindowLifecycle.ts`
- `frontend/src/views/Overlay.tsx`

**验证方式**

- `npm run build`
- overlay 输入框尚未完全入场时点击空白或按 Esc，确认可以立即退场。

**后续注意**

浮窗应用的动效必须可打断。系统事件保护和用户主动操作不能混为一谈。

## 2026-07-08 玻璃 UI 中间层过深显脏

**现象**

Overlay 中输入框和胶囊按钮的玻璃材质中间区域接近黑色，视觉上显得脏。用户明确指出不是图标/品牌蓝色问题。

**根因**

多个玻璃背景使用接近黑色的底层色，例如 `rgba(9, 13, 25, ...)`、`rgba(8, 12, 24, ...)`、`rgba(6, 9, 18, ...)`，叠加 blur 和阴影后中间区域发黑。

**解决方案**

- 恢复图标/按钮品牌蓝色，不改变 accent 语义。
- 仅提亮玻璃背景的中间暗层和底色层，改为更干净的蓝灰透明层。
- 同步提亮任务状态 pill、step、empty state 的玻璃底色。

**涉及文件**

- `frontend/src/styles/components.css`
- `frontend/src/styles/tokens.css`

**验证方式**

- `npm run build`
- 对照 overlay 截图检查输入框和胶囊按钮中部不再接近黑色。

**后续注意**

调整视觉颜色前要先区分“品牌色/图标色”和“材质底色”。不要为了解决玻璃发脏误改 icon 或 action 色。

## 2026-07-21 知识库并发覆盖与本地接口安全边界不足

**现象**

并发编译、重复采集或同时审核时可能产生重复任务、孤立快照或旧提案覆盖新编辑；完整备份恢复若接受恶意 ZIP 路径可能写出知识库目录；浏览器桥接 WebSocket 未校验 Origin。

**根因**

知识库写操作此前只有进程内流程判断，没有跨线程/进程的原子资源锁；Proposal 未绑定目标内容版本；备份 manifest 未约束成员路径；本地服务默认信任所有 Host 和 WebSocket Origin。

**解决方案**

- 增加 SQLite 租约锁，对来源采集、编译、页面写入、审核和恢复按资源串行化。
- Proposal 持久化目标 Note SHA-256 和来源快照 ID，审核前执行乐观并发校验。
- 备份恢复验证每个成员的 SHA-256、相对安全路径和非符号链接属性。
- HTTP 增加 Trusted Host，浏览器桥接仅允许配置来源、Tauri 和 Chrome 扩展来源。
- 增加 24 路重复采集和 8 路并发审核测试。

**涉及文件**

- `backend/app/knowledge/locking.py`
- `backend/app/knowledge/compiler.py`
- `backend/app/knowledge/source_service.py`
- `backend/app/knowledge/backup.py`
- `backend/app/main.py`
- `backend/app/api/routes_browser.py`
- `backend/tests/test_knowledge_security.py`

**验证方式**

- `uv run pytest backend/tests/test_knowledge_security.py`
- `uv run pytest backend/tests`

**后续注意**

新增任何会修改知识文件、索引或 Profile 成员关系的入口，都必须声明资源锁键并同时验证数据库与文件两侧的一致性；导入归档格式不得直接信任成员路径。

## 2026-07-21 非模态浮窗三期 — 多形态面板（Quick / Conversation / HUD）

**现象**

将全屏浮窗重写为紧凑非模态面板（快速输入/对话/执行控制条三种形态），引入 Tauri 窗口动态尺寸切换和跨窗口事件通讯。代码审查发现 8 项问题。

**涉及文件**

- `frontend/src/views/Overlay.tsx`
- `frontend/src/views/windowActions.ts`
- `frontend/src/styles/components.css`
- `frontend/src-tauri/tauri.conf.json`

**发现列表**

1. HUD 形态下失败/取消任务仍显示绿色勾（Overlay.tsx:292）
   - 严重：高
   - `running ? <LoaderCircle/> : <CheckCircle2/>` 仅在 running 时显示加载图标，非 running 一律显示绿勾。mode 为 "failed"/"cancelled" 时 statusLabel 虽显示正确文字，但图标与状态冲突。
   - 修复：用 `mode === "completed"` 条件区分，失败/取消使用 XCircle 或 AlertCircle。

2. startNewConversation 未清除 streamError（Overlay.tsx:273）
   - 严重：高
   - SSE 断开后 streamError 被设置。用户点"新对话"仅清除 actionError，streamError 残留，新对话中仍显示"任务事件连接异常"。
   - 修复：startNewConversation 中增加 `setStreamError(null)`。

3. 窗口显示先于入场动画（windowActions.ts:95-96 / Overlay.tsx:130）
   - 严重：中
   - `showOverlayWindow()` 先调 `overlay.show()` 再 `emitTo("overlay-open-request")`。监听器将 `playOpenMotion()` 包在 `requestAnimationFrame` 中。窗口先以 CSS 默认不透明度 (1) 可见，约 16-33ms 后 GSAP 才施加 autoAlpha:0 开始淡入，产生闪烁。
   - 修复：emitTo 提前到 overlay.show() 之前发出，或 show 前预先设置 CSS opacity: 0。

4. 入场 GSAP timeline 被形态过渡 fromTo 覆盖（Overlay.tsx:146）
   - 严重：中
   - 第二个 useGSAP（依赖 `[shape, reduceMotion]`）在挂载时运行。`gsap.fromTo()` 默认 `immediateRender: true` 且因全局 `overwrite: "auto"` 覆盖了 timeline 中对同一 panel 的子动画。panel 从 autoAlpha:0.72 而非 0 开始动画。当前因挂载时窗口隐藏而未产生可见故障，但仍为潜在 bug。
   - 修复：第二个 useGSAP 增加早期返回守卫，或改用 timeline 统一管理。

5. restoreFloatingBall 轮询重试丢失（windowActions.ts:34-41）
   - 严重：中
   - 原 `restoreFloatingBall()` 实现了最多 5 次轮询（0/120/360/900/1800ms）加版本号守卫以应对 Tauri 窗口管理器竞争。新代码内联为单次 `await floatingBall?.show()` 且无重试，`restoreFloatingBall` 函数变为死代码。
   - 修复：保留轮询重试逻辑或确认 Tauri 版本已不需要。

6. 点击外部关闭行为完全移除（Overlay.tsx:183/186）
   - 严重：中
   - 旧代码通过 `onMouseDownCapture` 检查点击目标和 `closeOnBlur: true` 实现遮挡区域关闭。新代码设置 `closeOnBlur: false` 且删除 `onMouseDownCapture`，唯一的关闭路径只剩 X 按钮。
   - 修复：恢复外部关闭机制（点击非交互区域或窗口失焦时关闭）。

7. TaskExecutionPanel mode idle 守卫移除（Overlay.tsx:399）
   - 严重：低
   - 旧代码 `mode !== "idle" && hasVisibleTaskSteps` 守卫面板渲染。新代码仅靠 `visibleStepCount > 0`。状态过渡期间（clearEvents 后 mode='idle' 但事件未清空）可能短暂渲染带默认标题的旧步骤。
   - 修复：恢复 `mode !== "idle"` 守卫。

8. 覆盖窗口生命周期依赖单一脆弱事件路径（Overlay.tsx:183）
   - 严重：低
   - `showOverlayWindow` 通过 `emitTo("overlay-open-request")` 通知 overlay。如果该事件静默丢失（如 WebView 监听器尚未注册），overlay 打开后不会播放动画且无法恢复。无重试或回退机制。
    - 修复：结合定时回退或 useWindowLifecycle 的 onOpen 备用通道。

## 2026-07-27 设置页异步内容保持隐藏

- 现象：个人信息区域已渲染且可被无障碍树读取，但 Playwright 判断不可见；账号页其他带 `settings-page-item` 的元素也保持透明。
- 根因：设置页打开动画和页面项目动画同时对同一批元素使用 `gsap.from(autoAlpha: 0)`。后执行的动画把前一动画的 0 当成终态，导致透明度无法恢复。
- 解决方案：窗口打开动画不再控制页面项目；页面项目改用确定性的 `fromTo(0 → 1)`，结束后清理 opacity/visibility 内联样式。
- 涉及文件：`frontend/src/views/Settings.tsx`。
- 验证方式：前端构建通过；Playwright 3/3 通过，其中个人信息用例检查标题、候选值及确认请求均可见可操作。
- 后续注意：同一元素只能由一个入场时间线拥有；异步渲染列表应使用明确终态并在完成后清理内联可见性样式。

## 2026-07-27 Tauri 安装包未显式声明 Windows 图标

- 现象：桌面程序成功编译后，Tauri 在 MSI 打包阶段报告 `Couldn't find a .ico icon`。
- 根因：仓库已有有效的 `src-tauri/icons/icon.ico`，但 `tauri.conf.json` 的 bundle 配置没有显式声明图标资源。
- 解决方案：在 `bundle.icon` 中声明 `icons/icon.ico`。
- 涉及文件：`frontend/src-tauri/tauri.conf.json`。
- 验证方式：`npx tauri build` 通过，成功生成 MSI 与 NSIS 两种安装包。
- 后续注意：新增其他平台安装包时，应同时补齐该平台所需的 PNG/ICNS 图标资源。

## 2026-07-27 个人信息窗口出现灰色外围边缘

- 现象：独立个人信息窗口四周存在一圈灰色背景，并叠加圆角边框和阴影，与无边框桌面窗口的视觉预期不符；分类内容全部纵向堆叠，查找和切换不直观。
- 根因：页面根容器设置了 14px padding 和灰色背景，内部 shell 又设置了圆角、边框与阴影；分类缺少独立的导航状态。
- 解决方案：根容器改为贴边白色画布，移除 shell 外框、圆角和阴影；新增左侧分类导航、数量、待确认提醒、当前分类标题、空状态和上下文新增逻辑。
- 涉及文件：`frontend/src/views/PersonalInfoSettings.tsx`、`frontend/src/styles/components.css`、`frontend/e2e/personal-info.spec.ts`。
- 验证方式：920×700 实际页面截图检查无灰色外围；前端构建通过；Playwright 4/4 通过，并验证分类切换、待确认筛选与根容器样式。
- 后续注意：无 decorations 的 Tauri 页面不要再在根层模拟第二层窗口边框；内容分组超过 4 类时应使用固定导航而非长列表堆叠。

## 2026-07-27 个人信息窗口只能打开一次

- 现象：个人信息窗口第一次可以打开，关闭后再次从悬浮球菜单打开时页面不可见，表现为卡死。
- 根因：Tauri 的 hide/show 不会卸载 React WebView。关闭动画把根节点留在 `opacity: 0` 和 `visibility: hidden`，个人信息页既没有像设置页一样在隐藏后复位动效状态，又显式关闭了 `replayOnFocus`，第二次显示时复用了隐藏节点。
- 解决方案：新增统一的个人信息动效复位函数，在窗口隐藏完成后清除 opacity、visibility 和 transform；重新启用窗口获得焦点时的入场动画，形成复位与重播双重保障。
- 涉及文件：`frontend/src/views/PersonalInfo.tsx`、`frontend/e2e/personal-info.spec.ts`。
- 验证方式：新增连续两次关闭循环的回归测试，检查每次关闭动画完成后根节点均恢复可见；前端构建通过、Playwright 5/5 通过、`cargo check` 通过。
- 后续注意：所有采用 hide/show 的持久 Tauri 窗口必须在关闭动画后恢复 DOM 终态，不能依赖 React 重新挂载。

## 2026-07-27 校招复杂表单只保存手机号

- 现象：真实校招网申表单填写完成后仅识别并保存了电话号码；姓名、邮箱、自定义下拉以及教育经历中的学校、时间、学院、专业、学历均未形成可复用资料。
- 根因：旧协议只采集可见原生 input/textarea/select，标签识别依赖控件附近的 label；组件化两栏布局和 Ant Design 等自定义下拉不满足条件。后端模型又要求 field_key 全局唯一，无法表达“一条教育经历包含多个字段”或多条教育经历。
- 解决方案：扩展增加视觉邻近标签、章节、重复记录索引、常见自定义下拉/单选识别与异步选项点击；后端新增关联记录/子字段表，将教育和工作经历成组保存并按记录序号回填；个人信息页面增加关联记录卡片和字段级编辑。
- 涉及文件：`browser-extension/src/content.ts`、`backend/app/db/connection.py`、`backend/app/personal_info/service.py`、`backend/app/api/routes_personal_info.py`、`frontend/src/views/PersonalInfoSettings.tsx` 及相关测试。
- 验证方式：与用户截图等价的 DOM 夹具识别姓名、邮箱、最高学位和完整教育经历，并成功回填自定义学历下拉；两条教育经历关联保存/按序回填测试通过；后端 106/106、Playwright 6/6、前端和扩展构建、`cargo check` 均通过。
- 后续注意：更新后必须重新加载浏览器扩展并重启 DeskPilot 以创建新数据表；此前只保存的手机号不会自动推断出其余字段，需要在原表单上重新执行一次“记住这个表单”。

## 2026-07-27 无模型降级拒绝“记录表单信息”

- 现象：用户在已绑定网页表单的情况下输入明确指令“记录表单信息”，系统仍提示语义模型不可用且请求不可安全降级。
- 根因：确定性降级规则只识别“记住/保存表单”，遗漏“记录/收录”同义表达；Manager、Web Agent 和工具确认各自维护了不一致的关键词列表。
- 解决方案：新增共享表单意图判断，统一覆盖“记住、保存、记录、收录”，并同时用于路由、无模型执行和写入确认；补充工具说明中的自然语言示例。
- 涉及文件：`backend/app/agent/form_intent.py`、`backend/app/agent/intents.py`、`backend/app/agent/specialists/base.py`、`backend/app/agent/specialists/web_agent.py`、`backend/app/tools/browser/form_autofill.py`。
- 验证方式：以用户原句“记录表单信息”回归，确认在有浏览器上下文时委派 Web Agent、操作类型为保存且通过写入确认；无浏览器上下文仍安全提示先打开目标表单。
- 后续注意：新增表单指令同义词时只修改共享词表，避免路由成功但执行或确认阶段再次拒绝。

## 2026-07-27 表单已写入但个人信息窗口不显示新增内容

- 现象：表单任务回复已记住 19 项，但重新打开个人信息库后看不到任何新增内容。
- 根因：个人信息窗口采用 Tauri hide/show 长期复用，列表只在 React 首次挂载时请求一次 API，后续打开仍显示保存前的内存缓存；任务回复又把识别到的控件次数描述成最终保存条数，重复控件和同义字段合并后造成数量误解。
- 数据核验：本次运行数据库实际已写入并可由 `/personal-info` 返回 7 个唯一字段，更新时间与任务一致，并非写入事务失败。
- 解决方案：个人信息窗口每次重新获得焦点时触发资料刷新；保存结果新增识别控件数、唯一落库字段数、新增数和合并/更新数，回复中分别展示。
- 涉及文件：`frontend/src/views/PersonalInfo.tsx`、`frontend/src/views/PersonalInfoSettings.tsx`、`backend/app/personal_info/service.py`、`backend/app/tools/browser/form_autofill.py` 及相关测试。
- 验证方式：后端全量测试 107/107、个人信息 Playwright 4/4 及前端生产构建通过；新增用例模拟持久窗口首次读取空列表、后台写入后再次打开并显示新资料。
- 后续注意：所有 hide/show 持久窗口中的服务端数据都必须在 onOpen 时重新同步；采集数量不得直接等同于唯一资料数量。

## 2026-07-27 Phoenix 校招表单标签错位且教育经历未分组

- 现象：真实智联 Phoenix 表单识别出 19 个非空控件，但姓名之后的标签和值发生错位，两条教育经历均落入 `general:0`，最终只形成少量被反复覆盖的普通字段。
- 根因：页面章节标题使用混淆 CSS 类而非 heading 标签，通用视觉标签算法在复杂两栏布局中会取到相邻字段；扩展没有利用输入框自带的 `data-nc-cls`（章节）、`data-nc-label`（字段）和 `data-nc-group`（重复记录序号）。字段分类的同分规则也会让长标签“招聘渠道”被较短的“职位”先匹配。
- 解决方案：优先读取当前 `.form-item` 的直接结构标签，并使用 `data-nc-*` 作为稳定语义元数据；将 1 基的 group 转为 0 基关联记录索引；字段分类同分时选择更具体的长别名；关联记录子字段按固定语义顺序返回，避免同时间写入时被随机 UUID 打乱。
- 验证方式：使用用户提供的完整 57KB DOM 实跑修复前与修复后输出；修复后 19 个非空控件形成 5 个个人字段和两条各含 7 个字段的教育经历。新增等价 Phoenix DOM 回归夹具；扩展构建、Playwright 8/8、后端 107/107 通过。
- 后续注意：站点语义属性只作为页面提供的数据读取，不依赖动态混淆类名；结构标签与属性冲突时以当前字段容器内的可见标签为准。

## 2026-07-28 表单候选值可能进入外部模型观察

- 现象：智能填写工具把完整填写计划作为普通工具结果返回，其中包含姓名、联系方式、经历正文和开放题答案；通用 observation 归一化随后可能把这些值送入已配置的外部语义模型。
- 根因：首版实现复用了“工具结果既供模型推理、又供前端展示”的单一数据通道，没有为本地敏感交互数据建立独立读取边界。
- 解决方案：完整候选计划只写入有 30 分钟有效期的本地 `form_fill_sessions`；Agent observation 和任务事件仅暴露一次性会话 ID、分类计数及是否需要复核。前端确认面板再通过 `127.0.0.1` 本地 API 按会话 ID 读取候选。记忆表单工具的返回值也只保留计数，不再返回已保存的实际字段值。
- 涉及文件：`backend/app/personal_info/service.py`、`backend/app/tools/browser/form_autofill.py`、`backend/app/api/routes_personal_info.py`、`frontend/src/api/client.ts`、`frontend/src/views/FormFillReview.tsx`、`frontend/src/views/Overlay.tsx`。
- 验证方式：新增回归断言 Agent 可见的会话引用不包含 `fields`、`groups` 或测试姓名，而本地会话 API 仍能返回确认界面所需内容；个人信息专项测试、前端构建均通过。
- 后续注意：任何含个人资料、文件正文或页面输入值的交互数据，都不能因为前端需要展示而默认复用模型 observation；必须明确区分“模型摘要”和“本地详情”。

## 2026-07-28 表单确认后可能退回当前活动标签页

- 现象：填写会话持久化后，标签页 ID 以 SQLite TEXT 返回；浏览器扩展只接受数字类型的目标 ID，因此可能退回当前活动标签页。若同一标签页发生 SPA 跳转，旧实现又只在动作完成后校验 URL，存在先触碰错误页面再报错的风险。
- 根因：后端到扩展的目标协议没有在持久化边界恢复 ID 类型，浏览器后台也缺少动作派发前的 URL 校验；同 URL 刷新无法仅凭标签页与 URL 区分。
- 解决方案：会话应用时把纯数字标签页 ID恢复为 number；浏览器后台在向 content script 发指令前校验目标 URL；content script 为每次文档实例生成 ID，并在任何字段写入前核对预览携带的实例 ID。扩展仍会逐字段核对预览时的当前值，默认禁止覆盖。
- 涉及文件：`backend/app/api/routes_personal_info.py`、`backend/app/browser/bridge.py`、`browser-extension/src/background.ts`、`browser-extension/src/content.ts`。
- 验证方式：Playwright 回归覆盖无明确替换不覆盖、预览值变化拒绝覆盖、文档实例一致时才写入，且结果始终为 `submitted: false`；浏览器扩展构建通过。
- 后续注意：所有有副作用的浏览器命令必须在执行前完成 tab、URL、document 三层校验；执行后校验只能作为审计兜底，不能作为唯一防线。

## 2026-07-28 右键菜单存在无效操作且键盘导航不完整

- 现象：悬浮球菜单中的“任务历史”和“退出 DeskPilot”没有绑定行为；文件右键菜单与悬浮球菜单采用两套视觉和结构，且仅支持 Tab，缺少上下方向键、Home/End 等标准菜单导航。
- 根因：两处菜单分别以临时按钮列表实现，没有共享信息架构、菜单语义和焦点管理；退出动作也未配置 Tauri 窗口关闭权限。
- 解决方案：新增统一 `AppContextMenu` 原语，统一玻璃材质、分组、危险色、焦点与 GSAP 动效；悬浮球菜单调整为打开主面板、知识库、个人信息、设置和退出，移除无实现的历史入口；文件菜单补充复制路径，并接通真实的应用退出链路。
- 涉及文件：`frontend/src/views/AppContextMenu.tsx`、`ContextMenu.tsx`、`ArtifactLinks.tsx`、`windowActions.ts`、菜单样式与 Tauri 窗口配置。
- 验证方式：224×230 实际视口截图无裁切；菜单 Playwright 回归 2/2 通过；前端生产构建和独立 target 的 Tauri debug 构建通过。
- 后续注意：新增右键菜单必须复用统一原语；任何可见菜单项都必须具备可验证的实际行为，不能以无响应按钮作为占位。
